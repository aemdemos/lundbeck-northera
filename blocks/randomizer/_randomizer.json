{
  "definitions": [
    {
      "title": "Randomizer",
      "id": "randomizer",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Randomizer",
              "model": "randomizer"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "randomizer",
      "fields": [
        {
          "component": "select",
          "valueType": "string",
          "name": "type",
          "label": "Type",
          "value": "",
          "description": "Renders the default-content rows below using another block's own layout (author those rows exactly as you would in that block's own table). Leave blank for plain content.",
          "options": [
            { "name": "None (plain content)", "value": "" },
            { "name": "Hero Patient", "value": "hero-patient" }
          ]
        },
        {
          "component": "aem-content",
          "valueType": "string",
          "name": "urls",
          "label": "Randomizer URLs",
          "description": "Alternate fragments to swap in at random. Do not include the default variant — author that directly as this block's own content rows.",
          "multi": true
        }
      ]
    }
  ],
  "filters": []
}
